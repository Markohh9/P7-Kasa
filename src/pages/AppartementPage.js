import { useParams } from "react-router-dom";

export default function AppartementPage() {

    const params = useParams()
    console.log(params);

    return (
        <div>

        </div>
    )

};